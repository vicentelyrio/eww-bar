(defwidget wifi []
  (box
    :class "wifi"
    :orientation "horizontal"
    :space-evenly false
    (label :class "wifi-name" :text `${SSID}`)
    (label :class "wifi-icon" :text `${SIGNAL}`)
  )
)

